apiVersion: v1
kind: Pod
metadata:
  name: simple-nginx-pod
  labels:
    app: nginx
    environment: test
spec:
  containers:
  - name: nginx-container
    image: nginx:1.25
    ports:
    - containerPort: 80
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m"
      limits:
        memory: "128Mi"
        cpu: "500m"
